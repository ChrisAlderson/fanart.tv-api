{
  "name": "fanart.tv-api",
  "version": "1.0.3",
  "description": "A Fanart.tv API wrapper for NodeJS.",
  "dependencies": {
    "got": "^7.1.0"
  },
  "devDependencies": {
    "chai": "^4.1.0",
    "coveralls": "^2.13.1",
    "istanbul": "^0.4.5",
    "mocha": "^3.4.2"
  },
  "license": "MIT",
  "scripts": {
    "coveralls": "cat ./coverage/lcov.info | node node_modules/coveralls/bin/coveralls",
    "test": "istanbul cover --report lcovonly ./node_modules/.bin/_mocha -- --timeout 15000 --reporter spec test/**"
  },
  "engines": {
    "node": ">=6.3.0"
  },
  "keywords": [
    "fanart.tv",
    "fanart",
    "tv",
    "api"
  ],
  "main": "fanart.tv-api.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ChrisAlderson/fanart.tv-api.git"
  }
}
